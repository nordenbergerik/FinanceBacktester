# FinanceBacktester

## Run the application

Install the project into the active virtual environment, then start the API and frontend together:

```bash
pip install -e .[api]
start-app
```

Open http://localhost:4173. The API documentation is available at http://localhost:8000/docs.